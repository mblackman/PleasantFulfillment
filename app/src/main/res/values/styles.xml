<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorAccent">@color/color_secondary_dark</item>
        <item name="cardBackgroundColor">@color/color_secondary_dark</item>
        <item name="android:textColorPrimary">@color/color_text_on_primary</item>
        <item name="android:textColorSecondary">@color/color_text_on_secondary</item>
        <item name="materialButtonStyle">@style/AppButton</item>
    </style>

    <style name="AppButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/color_text_on_secondary</item>
    </style>
</resources>